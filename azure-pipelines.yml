# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

jobs:
  - job:  
    pool:
      lens-build-agent
    steps:
      - script: npm i
      - script: npx nx affected:apps --base=HEAD~1
      - script: npx nx affected --target=build --base=HEAD~1
